# رفع نظام العيادة واختباره على GitHub

## محتويات المستودع

الفولدر جاهز كمستودع كامل ويحتوي على:

- `frontend`: واجهة React العربية RTL.
- `backend`: خادم Express وواجهات API والترحيلات والاختبارات.
- `shared`: الأنواع المشتركة.
- `backend/migrations`: مخطط PostgreSQL الكامل وتحديثاته.
- `render.yaml`: إعداد نشر التطبيق كـWeb Service على Render.
- `.github/workflows/ci.yml`: بناء واختبار تلقائي داخل GitHub Actions.

## الرفع إلى GitHub

ارفع **محتويات الفولدر** إلى جذر المستودع، وليس الفولدر الأب. بعد الرفع يجب أن ترى في الصفحة الرئيسية للمستودع:

```text
.github/
backend/
frontend/
shared/
package.json
package-lock.json
render.yaml
```

يجب أن يكون `frontend/index.html` موجوداً بهذا الاسم والمسار بالضبط.

## مشاهدة نتيجة الاختبار

1. افتح تبويب **Actions** في مستودع GitHub.
2. اختر **Build and test medical clinic**.
3. انتظر ظهور علامة النجاح الخضراء أمام **Verify full application**.
4. من قسم **Artifacts** يمكنك تنزيل `medical-clinic-production-build`.

الاختبار يعمل بقاعدة PostgreSQL محلية مؤقتة داخل جهاز GitHub ولا يحتاج إلى أسرار أو بيانات مرضى حقيقية.

## ربط قاعدة الإنتاج

GitHub يحفظ الكود ويختبره، لكنه ليس خادم قاعدة بيانات ولا يشغّل تطبيق Express بواسطة GitHub Pages. لتشغيل النظام الكامل:

1. اربط المستودع بخدمة **Web Service** على Render باستخدام `render.yaml`.
2. أنشئ قاعدة PostgreSQL في Neon.
3. ضع رابط الاتصال الكامل في متغير `DATABASE_URL` داخل Render فقط.
4. لا تضع رابط الاتصال في GitHub ولا في أي ملف داخل المستودع.

عند أول تشغيل ينفذ الخادم جميع ملفات `backend/migrations` تلقائياً ثم يضيف البيانات المرجعية اللازمة.

## الهاتف واللابتوب

الواجهة متجاوبة وتعمل من متصفحات الهاتف والتابلت واللابتوب. بعد نشر التطبيق على Render، افتح رابط `https://...onrender.com` نفسه من أي جهاز متصل بالإنترنت وسجّل الدخول بالحساب المحفوظ في قاعدة الإنتاج.

لا تستخدم رابط `localhost` على الهاتف؛ هذا الرابط يعمل على الجهاز الذي يشغّل المشروع محلياً فقط. رابط Render أو النطاق المخصص هو الرابط الذي يُستخدم من جميع الأجهزة.

## ما لا يجب رفعه

- ملف `.env`.
- مجلدات `node_modules`.
- مجلد `backend/data/clinic-db` المحلي.
- أي نسخة احتياطية تحتوي على مرضى حقيقيين.
- رابط `DATABASE_URL` أو كلمات المرور.
